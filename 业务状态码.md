## TravelMate 错误码规范文档

### 一、错误码格式说明

| 组成部分        | 说明                                            |
| :-------------- | :---------------------------------------------- |
| **HTTP 状态码** | 标准 HTTP 状态码（200/400/401/403/404/409/503） |
| **业务错误码**  | 5 位数字，格式：`{分类}{序号}`，如 `40401`      |
| **错误信息**    | 面向用户的可读信息，前端可原样展示或自定义映射  |

**格式示例**：

json

```
{
  "code": 40401,
  "message": "会话不存在或已过期",
  "details": {
    "thread_id": "thread_xxx"
  }
}
```



------

### 二、错误码分类表

| 分类               | 范围              | 说明                           |
| :----------------- | :---------------- | :----------------------------- |
| **系统/服务错误**  | `50000` - `50999` | 服务器内部错误、外部服务不可用 |
| **认证/权限错误**  | `40100` - `40399` | 身份验证失败、无权操作         |
| **客户端参数错误** | `40000` - `40099` | 请求参数格式错误、校验失败     |
| **资源不存在**     | `40400` - `40499` | 请求的资源未找到               |
| **流程/状态冲突**  | `40900` - `40999` | 当前状态不允许执行该操作       |

------

### 三、完整错误码清单

#### 3.1 系统/服务错误（50000-50999）

| 错误码  | HTTP 状态 | 说明                              | 前端处理建议                               |
| :------ | :-------- | :-------------------------------- | :----------------------------------------- |
| `50001` | 500       | 未捕获的系统内部错误              | 提示“系统繁忙，请稍后重试”，并上报日志     |
| `50301` | 503       | LLM API 不可用或超时              | 提示“AI 服务暂时不可用，请稍后重试”        |
| `50302` | 503       | 数据库连接失败（SQLite/ChromaDB） | 提示“数据服务异常，请检查网络或联系管理员” |

#### 3.2 认证/权限错误（40100-40399）

| 错误码  | HTTP 状态 | 说明                             | 前端处理建议                     |
| :------ | :-------- | :------------------------------- | :------------------------------- |
| `40101` | 401       | 缺少用户身份标识（`X-User-Id`）  | 提示“身份验证失败，请重新登录”   |
| `40102` | 401       | Token 无效或已过期               | 跳转登录页，刷新 Token           |
| `40301` | 403       | 无权操作该资源（归属权校验失败） | 提示“您无权访问该会话”，返回首页 |

#### 3.3 客户端参数错误（40000-40099）

| 错误码  | HTTP 状态 | 说明                              | 前端处理建议                              |
| :------ | :-------- | :-------------------------------- | :---------------------------------------- |
| `40002` | 400       | 请求参数校验失败（Pydantic 校验） | 高亮对应表单项，显示 `details.field` 错误 |
| `40003` | 400       | 参数类型/枚举值错误               | 检查下拉框/输入框限制                     |
| `40004` | 400       | 预算下限大于上限                  | 提示“预算下限不能高于上限”                |
| `40005` | 400       | 修改操作缺少具体指令（`hint`）    | 提示“请输入具体的修改诉求”                |
| `40006` | 400       | 决策类型（`action`）非法          | 检查前端枚举值映射                        |
| `40007` | 400       | 添加偏好缺少内容（`content`）     | 提示“请填写偏好内容”                      |

#### 3.4 资源不存在（40400-40499）

| 错误码  | HTTP 状态 | 说明                      | 前端处理建议                               |
| :------ | :-------- | :------------------------ | :----------------------------------------- |
| `40401` | 404       | 会话（`thread_id`）不存在 | 提示“会话已过期，即将刷新”，自动新建会话   |
| `40402` | 404       | 行程快照不存在            | 提示“该行程已被删除或不存在”，返回历史列表 |
| `40403` | 404       | 用户偏好标签不存在        | 列表自动刷新，忽略该条目                   |

#### 3.5 流程/状态冲突（40900-40999）

| 错误码  | HTTP 状态 | 说明                               | 前端处理建议                    |
| :------ | :-------- | :--------------------------------- | :------------------------------ |
| `40901` | 409       | 当前会话未处于中断状态（无法恢复） | 禁用“确认/修改”按钮，或静默忽略 |