## 开发计划

### 📅 P0 阶段（第 1-2 周）：核心闭环

**目标**：跑通“自然语言对话 → 行程生成 → 预算核算 → 校验 → 流式输出”的完整链路。

**实现功能**：

- □ 

  FastAPI 项目初始化，配置日志和异常处理

- □ 

  定义 `TravelAgentState` 核心状态（TypedDict）

- □ 

  搭建 LangGraph 基础图结构（Supervisor + Itinerary Agent + Budget Agent + Harness Validator + 结束路由）

- □ 

  实现 Supervisor Agent（意图识别 + 任务分发）

- □ 

  实现 Data Pre-fetcher（并发调用天气/景点 API，含 Redis 缓存读写）

- □ 

  实现 Itinerary Agent（PLAN 模式，生成 `daily_itinerary`）

- □ 

  实现 Budget Agent（读取行程，计算 `budget`）

- □ 

  实现 Harness Validator（硬规则校验：预算范围、行程非空）

- □ 

  集成 `SqliteSaver` 实现状态持久化

- □ 

  实现 `/api/v1/chat/stream` 接口（SSE 流式响应）

- □ 

  实现 `/api/v1/chat/stop/{thread_id}` 接口（停止生成）

- □ 

  实现 `/api/v1/trips/{thread_id}` 接口（获取行程快照）

- □ 

  实现 `/api/v1/health` 健康检查接口

- □ 

  建立 Redis 连接池和缓存工具类

- □ 

  建立统一错误码体系（40001~50301）

**测试验证**：

- □ 

  测试用例：输入“帮我规划北京3日游，预算中等” → 正常生成完整行程 + 预算，SSE 逐字返回

- □ 

  测试用例：输入“我想去个凉快的地方” → Supervisor 反问“您想去哪？几天？”

- □ 

  测试用例：输入无效目的地（如“火星”） → 返回友好错误提示

- □ 

  测试用例：调用 `/chat/stop` 后，流式输出立即中断，`stop_flag` 写入 Redis

- □ 

  测试用例：刷新页面后调用 `/trips/{thread_id}` 能正确恢复行程数据

- □ 

  测试用例：Redis 缓存命中（第二次规划同一城市时，API 调用次数减少）

- □ 

  测试用例：SQLite 状态持久化（重启应用后，相同 `thread_id` 可恢复历史对话）

### 📅 P1 阶段（第 3-4 周）：差异化亮点

**目标**：实现核心差异化功能——动态增量修改、人机协同、长期记忆。

**实现功能**：

- □ 

  实现 Itinerary Agent 的 REPLAN 模式（增量修改逻辑 + 时间锚定）

- □ 

  实现 Supervisor 对 PLAN/REPLAN 模式的自动识别

- □ 

  实现 Harness Validator 软评估（LLM 辅助判断行程合理性，含重试计数器）

- □ 

  实现 `interrupt` 人机协同中断（预算超支时触发）+ 前端决策面板数据格式

- □ 

  实现 `/api/v1/chat/resume` 接口（恢复中断流程）

- □ 

  集成 ChromaDB 嵌入式模式，实现语义长期记忆（用户偏好向量存储 + 检索）

- □ 

  实现用户操作日志 RAG（记录“用户删除了恭王府”等行为，向量化存储）

- □ 

  在 Itinerary Agent 的 PLAN 模式中注入记忆检索结果

- □ 

  实现 `/api/v1/trips` 接口（获取历史会话列表）

- □ 

  支持 `structured_input` 表单数据注入 State

**测试验证**：

- □ 

  测试用例：首次规划生成完整行程后，用户输入“第二天太累了，改成轻松点” → 仅 Day 2 及之后的行程被修改，Day 1 保持不变

- □ 

  测试用例：用户输入“删掉恭王府，换成什刹海划船” → 精准替换对应条目

- □ 

  测试用例：预算超支时，SSE 发送 `interrupt` 帧，前端展示确认面板

- □ 

  测试用例：用户点击“压缩预算”后，调用 `/resume`，Agent 重新生成优化方案

- □ 

  测试用例：用户第二次规划时，Agent 自动应用历史偏好（如“不喜欢早起”体现在行程时间上）

- □ 

  测试用例：用户多次删除“紧凑型”景点后，Agent 在未来规划中主动推荐宽松行程

- □ 

  测试用例：表单提交（含 `structured_input`）→ Agent 跳过反问，直接按表单参数生成行程

### 📅 P2 阶段（第 5-6 周）：增强体验

**目标**：完善用户体验，增加辅助功能，准备面试演示材料。

**实现功能**：

- □ 

  实现多方案比选功能（生成 2-3 套行程方案供用户选择）

- □ 

  实现 `/api/v1/users/me/preferences` 接口（获取/更新用户偏好标签）

- □ 

  实现行程软删除功能（`DELETE /api/v1/trips/{thread_id}`）

- □ 

  实现基于用户行为日志的偏好演化（Agent 主动推断“该用户越来越偏好...”）

- □ 

  完善各接口的错误处理与日志记录

- □ 

  编写 Apifox 完整接口文档（含所有数据模型和错误码）

- □ 

  准备面试演示场景脚本（正常规划 / 应急修改 / 人机协同 / 记忆召回）

- □ 

  录制项目演示视频（备用，防止现场环境问题）

- □ 

  编写项目 README（含架构图、技术栈、快速启动指南）

**测试验证**：

- □ 

  测试用例：多方案比选功能返回至少 2 套可行方案

- □ 

  测试用例：用户手动修改偏好标签后，下次规划立即生效

- □ 

  测试用例：删除行程后，`/trips` 列表不再显示，`/trips/{thread_id}` 返回 404

- □ 

  测试用例：连续 3 次校验失败后，系统强制放行并携带警告，不陷入死循环

- □ 

  测试用例：从零开始跑通完整项目（`pip install` → Docker 启动 Redis → `uvicorn` → Apifox 调用），耗时 < 5 分钟

### 里程碑检查点

| 阶段        | 交付物          | 验收标准                                                     |
| :---------- | :-------------- | :----------------------------------------------------------- |
| **P0 结束** | CLI/Demo 可运行 | 能通过 Apifox 调通自然语言生成完整行程，SSE 流式输出，SQLite/Redis 正常持久化 |
| **P1 结束** | 核心功能完整    | 增量修改、人机协同、长期记忆三大亮点全部可演示，Apifox 接口文档覆盖所有 P0+P1 接口 |
| **P2 结束** | 面试就绪        | 项目可在陌生环境下 5 分钟内启动，演示脚本覆盖全部核心场景，README 完整 |





# 后续补充

现在这套封装足够覆盖天气/景点这类 JSON 缓存业务，但还不是“强一致缓存框架”。如果后面有高并发热点数据，可以再加防缓存击穿的锁、缓存版本号、批量读写等。

