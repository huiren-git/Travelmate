# 2026-08-20

- 落地 `/profile/settings` 的「重置本地缓存」(reset-cache) 功能：确认后删 localStorage key `travelmate-app-settings` + 重置 `useProfileOutletStore`/`useHistoryOutletStore` + `window.location.reload()`（兜底清 useState 草稿）。涉及 4 文件：`store/useAppSettingsStore.ts`(加 `resetLocalCache`)、`store/useProfileOutletStore.ts`、`store/useHistoryOutletStore.ts`(各加 `reset`)、`components/profile/SettingsOutlet.tsx`(给 `reset-cache` 绑 `onOk`)。`npx tsc -b` 通过。`export-history` 现已实现（见下条）；`logout-account` 仍为空壳。

- 落地 `/profile/settings` 的「导出全部历史行程」(export-history)：JSON 默认 + Excel(.xlsx) 可选，Modal 内嵌格式选择（`ExportFormatPicker` 自管 state 避免 confirm 不重渲染）。新增 `useProfilePageData.exportAllHistory(format)`：现场 `fetchAllSessions`+`fetchSessionSnapshot` 拉取并 `enrichTravelHistoryFromSnapshot` 补全（不依赖缓存），JSON 直接 Blob 下载，Excel 用 `xlsx`(`XLSX.writeFile`) 多 sheet（行程概览/每日路线/费用明细/每日费用）。`ProfileOutletPanel` 透传 `onExportHistory`。新增 i18n `settings.exportSuccess/exportEmpty/exportFailed`（zh+en）。**依赖新增 `xlsx`**（首次 `npm install` 因全局 cache `D:\nodejs\node_cache` EPERM 失败，改用 `--cache D:/travelmate/frontend/.npm-cache` 成功；`xlsx` 有 2 high 漏洞告警，未处理）。`npx tsc -b` 通过。

- **修复 `onExportHistory is not a function`**：根因不在 hook，而在 `layouts/ProfileLayout.tsx`——它接收完整 `useProfilePageData()` 返回值，却只用 `context={{...}}` 把字段**手列一遍**传给 `<Outlet>`，且那份手列清单漏掉了 `exportAllHistory`。于是 Outlet context 里没有该字段，`ProfileOutletPanel` 解构到 `undefined` → 点导出报 "is not a function"。修法：改为 `context={props}` 整体透传（不再手列），并把 `ProfileLayout` 签名从逐项解构改为 `props: ProfileLayoutProps`、仅解构 JSX 实际用到的 `profile`/`profileStats`（其余靠 `noUnusedLocals` 校验）。彻底消除「hook 新增字段但 layout 漏传」这类脆弱点。`npx tsc -b` 通过。
