# 2026-08-23 工作日志

## 预算超支人机协同修复（落地改动一~四 + 事前约束）
- 续上轮（2026-08-22 被 429 中断）完成 itinerary_agent.py prompt 预算硬约束规则插入（L659 前）。
- 全链路改动已落盘并 `py_compile` 通过：
  - state.py：声明 `budget_max_allowed: Optional[float]` + `budget_auto_retry: int`（L99/102）。
  - chat.py `_initial_state`：补 `"budget_max_allowed": None, "budget_auto_retry": 0`（L149-150）。
  - supervisor.py：schema 加 `budget_max`+`budget_scope`（L75-76）；`_normalize_decision` 人均×travelers 折算（L128-147，import get_travelers L21）；写回 state（L220-222）。
  - budget_agent.py：缺失时才按 budget_level 推导兜底、不覆盖已有上限（L144-165）。
  - itinerary_agent.py：`_state_payload` 注入 budget_max_allowed（L598）；prompt 加事前预算约束硬规则 + 措辞改为"不生成预算明细但须遵守上限"（L615/L659 前）。
- 优先级：文本预算(supervisor 抽) > budget_level 推导(budget_agent 兜底)。
- 覆盖场景：云南四日游"三人出行，预算一千块钱"→抽 1000→3人4天必超→should_trigger 返 True→interrupt→/chat/resume 生效。
- 未做（独立决策）：5%-20% 静默区（`budget_auto_retry` 死代码）。

## REPLAN 改预算重算 total（已落地）
- 用户确认"replan 改预算应重算 total 并重新生成行程"。核查：行程重生成已在做（_resolve_replan_scope 对"改预算"落 _scope_all→未来天重写）；缺口是 total 重算被 `should_run_budget_after_itinerary` 的 `plan` 模式硬门禁挡掉(validator.py:588)。
- 落地 loop-safe 方案：新增 `budget_dirty: bool` 状态字段。
  - state.py 声明 + chat.py `_initial_state` 补 False。
  - supervisor.py 写回 budget_max_allowed 时比对旧值（含从无到有）不同→置 budget_dirty=True。
  - validator.py 条件放宽：`(plan 且 无 budget) or (replan 且 budget_dirty)`。
  - budget_agent.py 重算后清 budget_dirty=False。
- 闭环安全论证：graph L135 budget_agent→validator 回边存在；二次进 validator 时 branch=state.next_node="budget_agent"→should_run_budget=False→is_finished=True→validator_router(L54) 短接 __end__，不死循环；且 `_collect_user_decision_requests` 用新 total/新 cap 重判 should_trigger。
- py_compile 5 文件全通过。

## 5%~20% 超支自动微调闭环（已落地）+ 第2项确认
- 用户选"实现自动微调闭环(原设计)"：消除 `budget_auto_retry` 死代码，超支先自动削行程、最多2次、仍超才中断。
- `BudgetOverrunHandler` 加 `overrun_ratio()` + `should_auto_retry()`(ratio∈[1.05,1.20)且 budget_auto_retry<2)；`should_trigger` 语义不变(<1.05 不中断/区间内 count≥2 中断/>1.20 中断)。
- `validator_node`：branch=budget_agent 且 passed 且 should_auto_retry → 置 `auto_reduce_budget=True`、递增 `budget_auto_retry`、`next_node=itinerary_agent`、`is_finished=False`(跳过 user_decision)；`should_run_budget_after_itinerary` 加 `or auto_reduce_budget` 让削减后重跑 budget_agent。新增 `_budget_auto_retry_needed`(import BudgetOverrunHandler)。
- `graph.py:validator_router` 在 auto_reduce_budget 真时豁免 attempts>=3 上限(循环由 budget_auto_retry≤2 + is_finished 约束，不死循环)。
- `itinerary_agent`：节点读 auto_reduce_budget 进"预算削减模式"(REPLAN 时 scope 强制 `_scope_all()` 全开放区)；`_auto_reduce_instruction(state)` 构造"total/上限/超出额/明细→砍最贵·降档·压非必要"指令进 payload+prompt(`_state_payload`/`_build_itinerary_messages` 加 auto_reduce_instruction 形参)。
- `budget_agent` 重算后清 `auto_reduce_budget=False` + `budget_dirty=False`。`state.py` 声明 `auto_reduce_budget: bool` + chat.py `_initial_state` 补 False。
- 闭环：validator(passB 超)→itinerary 削减→validator(passA)→budget 重算清标记→validator(passB 重判)；count<2 反复，count≥2 或仍>20% 则 interrupt；达标 is_finished 结束。py_compile 7 文件全通过。
- 第2项「REPLAN 只重写未来天」原已落地(2026-08-22 三桶机制：locked/preserved/editable，_is_locked_item 判 status∈LOCKED_STATUSES 或 _is_time_locked)；本次属确认，自动微调在 REPLAN 下复用该机制(scope 强制 _scope_all=全部未来可改天)，历史/进行中天不触动。

## 前端人机协同 UI 落地（落实 1-3 + 超支确认气泡，非弹窗）
- 背景：排查 thr_947c9343 发现后端已正确抛 budget_overrun interrupt（budget_max_allowed=100、total=4775 被正确抽取），但前端对 interrupt/resume 零引用，气泡接不到。
- 前端改动：
  - `api/chat.ts`：加 `resumeChat(thread_id, user_decision{action,hint?})`（复用 parseSseMessages 消费 SSE）+ 类型 `UserDecision(action∈accept|modify|reject)/ResumeChatRequest/BudgetInterruptPayload`。
  - `useChatPageState.ts`：加 `pendingInterrupt` 状态；`extractBudgetInterruptFromDone(event)` 遍历 `done` 事件 `data.tasks[].interrupts[].value.type==='budget_overrun'` 提取；`done` 分支写入；`resolveInterrupt(action,hint?)` 调 resumeChat 并消费 SSE 追加 assistant 气泡；返回对象暴露 pendingInterrupt/resolveInterrupt。
  - 新建 `components/chat/BudgetOverrunBubble.tsx`：assistant 风格气泡（max-w-[80%] rounded-2xl，复用 ChatMessages 样式），展示 description + 接受(accept)/调整(modify，展开 textarea 输入 hint)按钮；**非弹窗**。
  - `ChatLayout.tsx`：解构 pendingInterrupt/resolveInterrupt/isStreaming，在 ChatMessages 后渲染气泡，isResolving 绑 isStreaming。
- 配套后端修复（关键）：`chat.py._jsonable` 对 PregelTask/Interrupt(无 model_dump/dict)退化为 str → SSE done 的 tasks 变字符串、前端提取失败。新增 `_serialize_interrupts(tasks)` 在 `_run_graph` 构造 snapshot 时抽取 `id/name/interrupts[].value`，SSE 不再退化为字符串。
- 契约注意：后端 `_validate_decision` 接受 action∈{accept,modify,reject}，modify 时 hint 必填；气泡"调整"发 modify+hint(空时按钮禁用)。handler `build_payload` option id 写 adjust 但与后端契约不符，前端直接发 modify。
- 验证：前端 `npx tsc --noEmit -p tsconfig.app.json`（经 node node_modules/typescript/bin/tsc）通过 EXIT=0；后端 chat.py py_compile 通过；`_serialize_interrupts` 端到端契约验证（真实 budget_overrun payload → 前端 extract 命中、无 str 退化）通过。
- 影响：thr_947c9343「预算100元→超支4775」现可真正弹气泡让用户 accept/adjust 并调 /chat/resume 续跑。
