```
frontend/
├── index.html
├── package.json
├── tsconfig.json
├── tsconfig.app.json
├── tsconfig.node.json
├── vite.config.ts
├── tailwind.config.js
├── postcss.config.js
├── .env                          # 前端环境变量（API_BASE_URL 等）
│
└── src/
    ├── main.tsx                  # 应用入口（ReactDOM.render）
    ├── App.tsx                   # 根组件（路由配置）
    ├── index.css                 # Tailwind 样式入口
    │
    ├── api/                      # 📡 API 请求层（与后端通信）
    │   ├── client.ts             # 全局 HTTP 客户端（fetch 封装 + 拦截器）
    │   ├── chat.ts               # 聊天 SSE 流式请求（/chat/stream, /chat/resume, /chat/stop）
    │   ├── trip.ts               # 行程 CRUD（/sessions, /trips/{id}, DELETE）
    │   ├── user.ts               # 用户偏好（GET/PUT /preferences）
    │   └── health.ts             # 健康检查（/health）
    │
    ├── pages/                    # 📄 页面组件（路由对应）
    │   ├── ChatPage.tsx          # 主对话页（双栏布局：左侧聊天 + 右侧行程卡片）
    │   ├── HistoryPage.tsx       # 历史记录页（无限滚动会话列表）
    │   ├── ProfilePage.tsx       # 个人中心页（偏好标签管理）
    │   └── SettingsPage.tsx      # 设置页（主题切换、模型选择预留）
    │
    ├── components/               # 🧩 可复用 UI 组件（按功能域拆分）
    │   ├── common/               # 通用组件
    │   │   ├── Layout.tsx        # 全局布局（顶部导航 + 侧边栏 + 内容区域）
    │   │   ├── Button.tsx        # 封装按钮（多态：主/次/危险/禁用）
    │   │   ├── Input.tsx         # 封装输入框
    │   │   ├── Spinner.tsx       # 加载动画
    │   │   ├── Toast.tsx         # 全局 Toast 通知
    │   │   └── Modal.tsx         # 通用模态框基座
    │   │
    │   ├── chat/                 # 对话组件
    │   │   ├── ChatContainer.tsx     # 对话区容器（消息列表 + 输入框）
    │   │   ├── MessageList.tsx      # 消息列表（渲染 User/AI 消息）
    │   │   ├── MessageBubble.tsx    # 单条消息气泡（支持 Markdown）
    │   │   ├── ThinkingDots.tsx     # “正在思考...” 动画
    │   │   ├── InputBox.tsx         # 文本输入框 + 发送/停止按钮
    │   │   └── QuickForm.tsx        # 快速规划表单（折叠面板）
    │   │
    │   ├── itinerary/            # 行程展示组件
    │   │   ├── ItineraryPanel.tsx   # 右栏行程面板（主容器）
    │   │   ├── DayPlan.tsx          # 每日行程折叠面板
    │   │   ├── ItineraryItem.tsx    # 单条行程项（时间线样式）
    │   │   └── BudgetPanel.tsx      # 预算仪表盘（饼图/进度条）
    │   │
    │   ├── sidebar/              # 侧边栏组件
    │   │   ├── HistorySidebar.tsx   # 历史会话列表（无限滚动）
    │   │   └── HistoryItem.tsx      # 单条历史会话条目
    │   │
    │   └── interrupt/            # 人机协同（Human-in-the-loop）
    │       ├── InterruptModal.tsx   # 中断弹窗（预算超支确认）
    │       └── ResumeHandler.tsx    # 用户决策处理逻辑
    │
    ├── hooks/                    # 🪝 自定义 React Hooks（业务逻辑复用）
    │   ├── useChat.ts            # 核心：管理 SSE 流、消息发送/接收、中断恢复
    │   ├── useThread.ts          # 管理 thread_id（生成/从 URL 读取/存储到 LocalStorage）
    │   ├── useHistory.ts         # 历史记录列表加载（游标分页 + 无限滚动）
    │   ├── usePreferences.ts     # 用户偏好标签 CRUD
    │   └── useLocalStorage.ts    # 本地存储工具（持久化 thread_id / 主题偏好）
    │
    ├── store/                    # 📦 全局状态管理（Zustand）
    │   ├── chatStore.ts          # 对话状态（messages, isLoading, isStreaming, currentThreadId）
    │   ├── tripStore.ts          # 行程状态（dailyItinerary, budget, validationReport）
    │   ├── historyStore.ts       # 历史记录状态（sessions, cursor, hasMore）
    │   ├── userStore.ts          # 用户偏好状态（preferences, loading）
    │   └── uiStore.ts            # UI 状态（theme, sidebarOpen, modalState）
    │
    ├── types/                    # 🧬 TypeScript 类型定义（对齐后端 OpenAPI 契约）
    │   ├── trip.ts               # 行程类型（ItineraryItem, DayPlan, BudgetDetail）
    │   ├── chat.ts               # 对话/SSE 帧类型（ThinkingFrame, ChunkFrame, InterruptFrame, EndFrame）
    │   ├── user.ts               # 用户偏好类型（PreferenceItem, UpdateRequest）
    │   ├── api.ts                # API 通用响应类型（ApiResponse, ErrorResponse）
    │   └── enums.ts              # 枚举类型（BudgetLevel, Pace, Interest, Transport）
    │
    ├── utils/                    # 🛠️ 工具函数
    │   ├── sseParser.ts          # SSE 数据流解析器（按行解析 "data: {...}"）
    │   ├── dateFormatter.ts      # 日期格式化（ISO → YYYY-MM-DD / 相对时间）
    │   ├── errorHandler.ts       # 全局错误码映射（将后端错误码转为用户友好提示）
    │   ├── storage.ts            # LocalStorage 封装（读写 / 序列化）
    │   └── markdown.ts           # Markdown 渲染配置（react-markdown 插件）
    │
    ├── constants/                # 📌 常量配置（硬编码 / 不常改的配置）
    │   ├── errorCodes.ts         # 错误码映射表（40401: "会话已过期"）
    │   ├── enums.ts              # 枚举显示配置（图标映射：budget_level → 标签/颜色）
    │   └── config.ts             # 应用配置（默认预算等级、默认天数、分页大小）
    │
    ├── layouts/                  # 🏗️ 布局组件（嵌套路由用）
    │   ├── MainLayout.tsx        # 主布局（顶部导航 + 侧边栏 + 内容）
    │   └── AuthLayout.tsx        # 简单布局（登录/注册用，暂留空）
    │
    └── assets/                   # 📁 静态资源
        ├── images/               # 图片资源（Logo, favicon）
        └── icons/                # SVG 图标（若使用自定义图标）
```

