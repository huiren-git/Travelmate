# 项目概述

一个基于 **多智能体协作（Multi-Agent）** 架构的智能旅游助手。它不仅能在出行前根据用户偏好生成个性化行程与预算，更核心的价值在于**旅途中**的实时感知与动态应变能力——当景区拥堵、天气突变或用户体力不支时，能增量式地调整后续计划，而非全量重算。

**核心特性**（直接用AI生成补充后面）

- 多代理协同：A2A架构
- 多模态服务：
- 双界面支持：Web / 命令行
- 模块化设计：易于扩展和维护
- 个性化推荐

# 功能需求和用户场景

用例图画流程？

# 技术栈

| 模块                       | 技术选型                        | 选型理由（写进文档）                                         |
| :------------------------- | :------------------------------ | :----------------------------------------------------------- |
| **核心语言**               | Python 3.10+                    | 对 LangChain/LangGraph 原生支持最好，生态成熟                |
| **AI应用框架**             | LangChain                       | 标准化 Tool 定义、Prompt 模板、模型 IOC（多模型切换）        |
| **多智能体编排**           | **LangGraph**                   | 核心优势：**状态驱动（Stateful）**、支持循环与条件路由、原生支持 `interrupt`（人机协同） |
| **状态持久化（检查点）**   | **SQLite (SqliteSaver)**        | 零配置，单文件存储，完美支持 LangGraph 状态中断与恢复，启动极快 |
| **长期语义记忆（向量库）** | **ChromaDB (嵌入式持久化模式)** | **保留向量检索能力**，用于用户偏好的语义匹配。以文件形式存储，无需独立服务，兼顾功能与轻量 |
| **分布式缓存**             | **Redis (redis-py 异步客户端)** | 高性能内存缓存，支持持久化和 TTL，`redis-py` 提供类 RedisTemplate 的完整操作 |
| **API服务**                | FastAPI                         | 高性能，原生支持 SSE（流式输出），方便前端对接               |
|                            |                                 |                                                              |

# 系统架构

- **4.1 整体架构分层**：
  - **接入层** (FastAPI) -> **调度层** (Supervisor) -> **执行层** (Workers) -> **数据层** (PG/Redis/外部API)。
  
  - ```
    ┌─────────────────────────────────────────────────────────────────┐
    │                        接入层 (API Layer)                       │
    │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐           │
    │  │  REST API   │  │ SSE Stream  │  │ 请求/响应   │           │
    │  │   (同步)    │  │  (流式)     │  │  契约管理   │           │
    │  └─────────────┘  └─────────────┘  └─────────────┘           │
    │                         FastAPI + Pydantic                     │
    └─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
    ┌─────────────────────────────────────────────────────────────────┐
    │                       调度层 (Orchestration Layer)              │
    │  ┌─────────────────────────────────────────────────────────┐   │
    │  │              Supervisor Agent（调度主管）                │   │
    │  │    意图识别 → 信息完整性判断 → 任务分发 → 结果汇总      │   │
    │  └─────────────────────────────────────────────────────────┘   │
    │                    LangGraph StateGraph + Checkpointer         │
    └─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
    ┌─────────────────────────────────────────────────────────────────┐
    │                        执行层 (Execution Layer)                 │
    │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐           │
    │  │ 数据预取节点 │  │ Itinerary   │  │  Budget     │           │
    │  │ (纯函数)    │  │ Agent       │  │  Agent      │           │
    │  │             │  │ (LLM核心)   │  │  (LLM辅助)  │           │
    │  └─────────────┘  └─────────────┘  └─────────────┘           │
    │  ┌─────────────┐  ┌─────────────────────────────────────┐      │
    │  │  Harness    │  │        工具调用层                    │      │
    │  │  Validator  │  │  天气API / 地图API / 酒店API         │      │
    │  └─────────────┘  └─────────────────────────────────────┘      │
    └─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
    ┌─────────────────────────────────────────────────────────────────┐
    │                        数据层 (Data Layer)                      │
    │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐           │
    │  │   SQLite    │  │  ChromaDB   │  │  Redis      │           │
    │  │  (检查点)   │  │  (向量记忆) │  │  (缓存)        │           │
    │  └─────────────┘  └─────────────┘  └─────────────┘           │
    └─────────────────────────────────────────────────────────────────┘
    ```
  
- **4.2 核心状态机设计 (StateGraph)**：
  
  - 展示 `TravelAgentState` 的字段定义
  
    ```
    class TravelAgentState(TypedDict):
        # 1. 会话与上下文（基础）
        messages: Annotated[List[BaseMessage], add_messages]  # 对话历史（自动追加）
        user_id: str
        thread_id: str
        
        # 2. 用户意图（输入层）
        destination: Optional[str]
        origin: Optional[str]               # GPS/IP 默认填充，可手动改
        duration: Optional[int]
        structured_preferences: Optional[Dict]  # 包含 budget, pace, interests, travelers
        
        # 3. 外部数据（感知层，由 Pre-fetcher 填充）
        weather_info: Optional[Dict]
        fetched_attractions: Optional[List[Dict]]
        
        # 4. 核心产出（认知层，由 Agent 生成）
        daily_itinerary: Optional[List[DayPlan]]  # 每个 DayPlan 包含 items
        budget: Optional[BudgetDetail]
        
        # 5. 流程控制（元认知层）—— 这是状态机的灵魂
        current_mode: Literal["plan", "replan"]   # 区分首次规划还是应急修改
        current_time: Optional[str]               # 动态修改时的“时间锚点”
        validation_attempts: int                  # 重试计数器（防死循环）
        validation_report: Optional[Dict]         # 校验错误/警告
        is_finished: bool
    ```
  
  - **解释 Reducer 机制**：如何使用 `add_messages` 优雅地追加对话历史。
  
- **4.3 多智能体协作图 (Graph)**：
  - 贴上你画的 **Supervisor-Worker** 流转图
  
    ```
    START
       │
       ▼
    ┌─────────────┐
    │ Pre-fetcher │  ← 并发获取天气/景点数据
    └─────────────┘
       │
       ▼
    ┌─────────────┐
    │ Supervisor  │  ← 意图识别，决定路由
    └─────────────┘
       │
       ├──────────────┬──────────────┐
       │              │              │
       ▼              ▼              ▼
    ┌──────────┐ ┌──────────┐ ┌─────────┐
    │Itinerary │ │ Budget   │ │   END   │
    │  Agent   │ │  Agent   │ │ (结束)  │
    └──────────┘ └──────────┘ └─────────┘
       │              │
       └──────┬───────┘
              ▼
    ┌─────────────┐
    │  Validator  │  ← 校验方案质量
    └─────────────┘
       │
       ├──────────┐
       │          │
       ▼          ▼
    Itinerary   END
    Agent       (结束)
    (重试)
    ```
  
    
  
  - **文字说明**：Worker 执行完**无条件返回 Supervisor**，由 Supervisor 决定下一步是调用新 Worker 还是结束（`FINISH`）。这种 **“中心化编排”** 模式降低了智能体间的耦合度。

# 数据结构

# 枚举类总结

## 预算等级 budget.level

| 枚举值    | 图标 | 前端显示文案（文字画像） | 特点/适用范围（后端 Prompt 映射逻辑）                        |
| :-------- | :--- | :----------------------- | :----------------------------------------------------------- |
| `economy` | 💰    | **经济型** · 高性价比    | **特点**：侧重省钱，住宿偏好青旅/快捷酒店（非核心区），餐饮以街边小吃/快餐为主。 **适用**：学生党、穷游爱好者、预算敏感型用户。 |
| `mid`     | 💰💰   | **中等型** · 舒适自由行  | **特点**：追求性价比与舒适平衡，住宿偏好中档连锁（全季/亚朵），餐饮以当地特色餐厅为主。 **适用**：**主流用户群体**（默认选项），大部分白领/情侣。 |
| `luxury`  | 💰💰💰  | **豪华型** · 品质度假    | **特点**：侧重体验，住宿偏好四星以上/精品设计酒店，餐饮倾向黑珍珠/必吃榜/私房菜。 **适用**：纪念日旅行、高端度假、商务出行。 |

## 行程节奏 (pace)

| 枚举值      | 图标 | 前端显示文案              | 特点/适用范围（后端编排逻辑）                                |
| :---------- | :--- | :------------------------ | :----------------------------------------------------------- |
| `intensive` | 🔥    | **紧凑型** · 特种兵式打卡 | **特点**：每日安排 4 个以上景点，早出晚归，景点间通勤时间压缩至最短。 **适用**：年轻人、体力充沛、以“打卡”为主要目的的用户。 |
| `relaxed`   | 🌿    | **轻松型** · 休闲度假     | **特点**：每日安排 2-3 个核心景点，睡到自然醒，预留充足的咖啡馆/休息时间。 **适用**：带老人/小孩的家庭游、休假放松的用户。 |

# 兴趣偏好 (interests) 

| 枚举值      | 图标 | 前端显示文案 | 特点/适用范围（景点推荐倾向）                                |
| :---------- | :--- | :----------- | :----------------------------------------------------------- |
| `history`   | 🏛️    | 历史古迹     | **推荐**：故宫、长城、博物馆、古遗址。 **适用**：喜欢历史故事、人文底蕴的用户。 |
| `culture`   | 🎭    | 民俗文化     | **推荐**：特色街区、非遗体验、寺庙祠堂、当地市集。 **适用**：喜欢深度体验当地风土人情的用户。 |
| `food`      | 🍜    | 美食探店     | **推荐**：网红餐厅、小吃街、夜市、特色早茶/夜宵。 **适用**：以“吃”为旅行第一动力的美食家。 |
| `nature`    | 🌲    | 自然风光     | **推荐**：山川、湖泊、公园、植物园。 **适用**：喜欢户外、徒步、摄影的用户。 |
| `shopping`  | 🛍️    | 购物血拼     | **推荐**：大型商圈、奥特莱斯、免税店、特色手信街。 **适用**：有明确购物清单或代购需求的用户。 |
| `art`       | 🎨    | 艺术展览     | **推荐**：美术馆、艺术中心、建筑设计打卡点。 **适用**：文艺青年、设计从业者。 |
| `nightlife` | 🌙    | 夜生活/酒吧  | **推荐**：酒吧街、Livehouse、深夜大排档。 **适用**：喜欢夜间社交、体验城市夜色的用户。 |

# 同行者类型 (travelers_type) 

| 枚举值   | 图标 | 前端显示文案     | 特点/适用范围（行程特殊考量）                                |
| :------- | :--- | :--------------- | :----------------------------------------------------------- |
| `adult`  | 🧑‍🤝‍🧑  | 成人/朋友/情侣   | **特点**：正常体力消耗，可安排爬山、长时间步行、夜生活。 **适用**：朋友结伴、情侣出游。 |
| `family` | 👨‍👩‍👧‍👦 | 亲子家庭（带娃） | **特点**：规避高风险项目，景点需包含儿童游乐设施，行程节奏自动切换为 `relaxed`。 **适用**：带 12 岁以下儿童出行的家庭。 |
| `senior` | 👴    | 长辈/老年团      | **特点**：规避爬山/暴走，景点间通勤优先打车，酒店需带电梯，每天仅安排 1-2 个大点。 **适用**：带父母长辈出行的用户。 |

## 酒店选型 (hotel_preference)

| 枚举值    | 图标 | 前端显示文案  | 特点/适用范围（预算核算映射）                                |
| :-------- | :--- | :------------ | :----------------------------------------------------------- |
| `economy` | 🛏️    | 快捷/青旅     | **标准**：人均 < 200 元/晚，如汉庭、如家、青旅床位。 **适用**：仅把住宿当作睡觉的地方。 |
| `mid`     | 🛏️    | 舒适连锁      | **标准**：人均 250-450 元/晚，如全季、亚朵、维也纳。 **适用**：追求干净、安全、地段便利。 |
| `luxury`  | 🛏️    | 豪华/精品酒店 | **标准**：人均 > 600 元/晚，四星级以上或设计师酒店。 **适用**：注重住宿体验、酒店即目的地。 |

## intercity_transport（城际交通）枚举

```
["flight", "high_speed_rail", "train", "coach", "self_driving"]
```

| 枚举值            | 前端显示文案 | 适用于               |
| :---------------- | :----------- | :------------------- |
| `flight`          | ✈️ 飞机       | 远程（>1000km）      |
| `high_speed_rail` | 🚄 高铁       | 中远程（300-1000km） |
| `train`           | 🚂 普通火车   | 中程（预算优先）     |
| `coach`           | 🚌 大巴       | 短途（<300km）       |
| `self_driving`    | 🚗 自驾       | 灵活出行             |

## local_transport（市内出行）枚举



# 功能点总结



# 问题总结

如果面试官问：“你为什么用 ChromaDB 而不是 pgvector？”

你可以这样回答（既承认了工程约束，又展现了技术判断力）：

> “我确实考虑过 PostgreSQL + pgvector，那在工业界是标准的解决方案。但考虑到这是一个**中型求职项目**，我需要平衡**技术展示**和**启动成本**。
>
> 我的考量是：
>
> 1. **保留核心价值**：我依然需要向量检索来实现‘语义化长期记忆’，而不是简单的关键词匹配，这是我项目的亮点之一。
> 2. **规避过度设计**：对于几千条量级的用户偏好数据，启动一个完整的 PostgreSQL 服务是‘杀鸡用牛刀’，会增加项目初期的启动门槛。
> 3. **最终选型**：因此我选择了 ChromaDB 的**嵌入式持久化模式**。它在功能上完全满足我的向量检索需求，同时和 SQLite 一样是单文件存储，做到了‘开箱即用’。未来如果项目数据量达到百万级，我可以无缝将其迁移到 pgvector 或 Qdrant，但在当前阶段，这是最具性价比的架构决策。”

当面试官问：“你为什么不把应急规划拆成一个单独的 Agent？”

你可以自信地回答：

> “这是一个关于 **‘上下文归属权’** 的架构决策。
>
> 如果拆分成两个 Agent，应急 Agent 只能拿到行程的**快照（Snapshot）**，却拿不到规划时的**约束条件与推理链**——它不知道用户为什么喜欢这个景点，也不知道酒店定在哪里。这会导致它做出的修改像‘盲人摸象’，很容易引入逻辑矛盾。
>
> 为了杜绝这种**信息孤岛**，我选择把规划和应急合并到同一个 Agent。为了避免上下文混乱，我在工程上做了**4层隔离**：通过 Supervisor 传入 **`mode`（PLAN / REPLAN）** 动态切换 Prompt；在状态中用 **时间锚定**和 **`status` 状态锁** 来保护已完成的条目；并用 **结构化输出** 保证返回格式统一。
>
> 最终效果是：这个 Agent 在 PLAN 模式下是‘建筑师’，在 REPLAN 模式下是‘装修工’。**工具不同、图纸不同，但看的是同一套承重墙结构（State）**，所以既不会混乱，又保证了修改的连贯性。”

Pre-fetcher要作为一个节点，而不是一个Tool？

> **第一，执行强制性。** 行程规划**必须**依赖实时天气和景点数据，这是一个硬性前置条件。如果设计成 Tool，由 LLM（Supervisor）自主决定是否调用，就存在‘忘记调用’或‘判断错误’的风险，导致后续 Agent 在数据缺失下盲目推理。作为 Node，它在流程上被强制前置执行，从根本上保障了数据的完整性。
>
> **第二，性能最优化。** Pre-fetcher 是一段纯 Python 异步函数，不涉及 LLM 调用。我可以在其内部轻松使用 `asyncio.gather` 实现天气、景点 API 的**真正并行**，毫秒级完成，**0 Token 消耗**。如果作为 Tool，Supervisor 至少需要 2 次 LLM 调用（决策 + 解析），既增加延迟，又浪费成本。
>
> **第三，职责单一化。** 我的设计原则是：**确定性 I/O 归节点，不确定性推理归工具。** Pre-fetcher 只负责‘采集原材料’，不负责‘决定是否采集’——这种确定性逻辑用代码表达，远比依赖 LLM 的概率性判断更可靠。

**面试话术（当面试官问为什么用连接池时）**：

> “引入 Redis 连接池主要是为了应对我项目中的 **异步并发场景**。
>
> `pre_fetcher` 节点会并行拉取天气和景点数据，如果没有连接池，每次缓存读写都要新建 TCP 连接，网络开销占 95% 以上。连接池让多个协程复用少量长连接，既规避了端口耗尽风险，又把缓存操作从毫秒级优化到了微秒级。
>
> 在我目前的设计中，`max_connections=20` 足够支撑单实例下的并发需求。即使未来流量增大，也可以通过调整这个参数而非重构代码来应对——它让系统具备了横向扩展的基础。”