### ✅ Validator 校验事项清单（完整版）

| 序号   | 校验维度         | 校验项                             | 具体逻辑 / 判断条件                                          | 严重程度 | 失败后的动作                          |
| :----- | :--------------- | :--------------------------------- | :----------------------------------------------------------- | :------- | :------------------------------------ |
| **1**  | **结构与存在性** | 行程是否为空                       | `daily_itinerary` 为 `None` 或空列表 `[]`                    | **错误** | 阻断流程，触发 `Itinerary Agent` 重试 |
| **2**  |                  | 是否存在空白天数                   | 某一天的 `items` 为空列表 `[]`                               | 警告     | 记录警告，流程继续                    |
| **3**  |                  | 是否存在重复景点                   | 全行程中 `activity` 字段出现重复（同名景点）                 | 警告     | 记录警告，流程继续                    |
| **4**  | **数值与预算**   | 预算总额是否为负数                 | `budget.total < 0`                                           | **错误** | 阻断流程，触发重试                    |
| **5**  |                  | 预算总额是否缺失                   | `budget.total` 为 `None` 或不存在                            | 警告     | 记录警告，流程继续                    |
| **6**  |                  | 预算明细与总额一致性               | `budget.detail` 各项之和与 `budget.total` 的差值 > 0.01（允许极小浮点误差） | **错误** | 阻断流程，触发重试                    |
| **7**  |                  | 预算等级与总额匹配（等级一致性）   | 根据用户 `budget_level`（`economy`/`mid`/`luxury`）计算**人均每日预算**： - `economy`：300–800 元/人/天 - `mid`：800–1500 元/人/天 - `luxury`：> 1500 元/人/天 若实际值低于区间下限或高于上限（`luxury` 无上限），触发警告 | 警告     | 记录警告，流程继续                    |
| **8**  | **时间与逻辑**   | 同一日行程项时间重叠               | 若 `item_A.start_time + item_A.duration > item_B.start_time`，视为时间冲突 | 警告     | 记录警告，流程继续                    |
| **9**  |                  | 用餐时段覆盖（午餐）               | 11:30–13:30 期间需安排午餐                                   | 警告     | 记录警告，流程继续                    |
| **10** |                  | 用餐时段覆盖（晚餐）               | 17:30–19:30 期间需安排晚餐                                   | 警告     | 记录警告，流程继续                    |
| **11** | **偏好与体验**   | 行程节奏是否匹配用户偏好（轻松型） | 用户 `pace = "relaxed"` 且 **日均景点数 > 4**                | 警告     | 记录警告，流程继续                    |
| **12** |                  | 行程节奏是否匹配用户偏好（紧凑型） | 用户 `pace = "intensive"` 且 **日均景点数 < 3**              | 警告     | 记录警告，流程继续                    |
| **13** | **流程控制**     | 校验重试计数器                     | 每次校验将 `validation_attempts` 增加 1                      | —        | 内部状态管理                          |
| **14** |                  | 强制放行机制                       | 当 `validation_attempts >= MAX_VALIDATION_ATTEMPTS`（默认 3）时，`is_finished = True`（强制放行） | —        | 避免死循环，保障服务可用性            |

------

### 校验项总结（快速概览）

| 类型                | 数量  | 说明                                                         |
| :------------------ | :---- | :----------------------------------------------------------- |
| **错误（Error）**   | 3 项  | 必须修复，否则流程会持续重试。包含：行程为空、预算为负、预算明细与总额不一致。 |
| **警告（Warning）** | 10 项 | 记录但不阻塞流程。覆盖重复景点、时间重叠、用餐时段、预算等级匹配、节奏匹配。 |
| **流程控制**        | 2 项  | 内部管理重试计数与强制放行。                                 |

------

### 维度覆盖度分析（用于文档说明）

| 评估维度       | 覆盖情况                                 |
| :------------- | :--------------------------------------- |
| **数据完整性** | ✅ 覆盖（空行程、空白天数、缺失预算总额） |
| **数值正确性** | ✅ 覆盖（预算负数、明细与总额一致性）     |
| **预算合理性** | ✅ 覆盖（预算等级与人均每日预算匹配）     |
| **时间可行性** | ✅ 覆盖（时间重叠、用餐时段覆盖）         |
| **用户体验**   | ✅ 覆盖（节奏匹配、重复景点）             |
| **系统健壮性** | ✅ 覆盖（重试计数器、强制放行）           |