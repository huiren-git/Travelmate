# 2026-08-12

## Chat 页 New Trip 行为改造
- 需求：点 New Trip 不再立即在左侧 Conversations 列表生成 "New Trip" 条目；改为只清空中间栏（显示空状态），用户发送第一条消息后才在左侧生成条目（标题取自首条消息）。
- 改动文件：`src/hooks/useChatPageState.ts`
  - `startNewTrip()`：移除 `setConversationList` 创建条目的逻辑，仅创建空消息 + 加入 `emptyTripPlanConversationIds`，把激活会话设为待创建 threadId（不在列表里）。
  - `sendMessage()`：发送前用 `isPendingNewTrip = !conversationList.some(id === threadId)` 判断是否为新会话，若是则按首条消息创建 `Conversation` 条目加入列表；标题沿用 `conversationTitleFromMessage`，状态取 `conversations[0].status`。
  - 后续 `setConversationList` 标题重命名逻辑简化为仅更新 `updatedAt`（标题已在创建时设置）。
- 验证：`tsc -b` 通过（exit 0）。页面层 ConversationSider / ChatLayout 无需改动。
